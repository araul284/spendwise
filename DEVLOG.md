## Day 1 — 2026-05-06
**Hours worked:** 3
**What I did:** Created public GitHub repo. Set up project with Next.js + TypeScript + Tailwind. Verified and recorded current pricing for all 8 tools. 
**What I learned:** Understanding entrepreneurial requirements for building applications that ship in real-time. Researching pricing plans for the different AI tools (I wasn't aware of most of the pricing for teams or entreprises). Writing conventional git commits.
**Blockers / What I'm stuck on:** None
**Plan for tomorrow:** Draft system architecture, build the spend input form, build the audit engine as pure logic.

## Day 2 — 2026-05-07
**Hours worked:** 4
**What I did:** 
    - Worked on the System Architecture. 
    - Switched my stack from Next.js to React + Vite. Setting up React router and Vite modules with the other existing files which had Next.js configuration.
    - Built the Spend Input form. 
    - Added the Audit Engine to calculate effective monthly cost and check for rules such as if the plan is justified, whether there is an IDE overlap, or redundancy.
**What I learned:** 
    - Using Mermaid to build system architecture and visualize the user flow diagram. 
    - Setting up the supabase for adding the audit results and capturing leads.
    - Setting up React and Vite.
**Blockers / What I'm stuck on:** The homepage UI looks generic with inconsistent color schemes that is blending with the overall background as well as there are bugs in the input forms.
**Plan for tomorrow:** Enhancing the UI. Work on the unit tests for audit engine (wrong plan, cheaper alternative, already-optimal case). Start with the first user interview.

## Day 3 — 2026-05-08
**Hours worked:** 2
**What I did:** Worked on refining the UI but couldn't tackle other tasks on my today's agenda as I was suffering from a very severe migraine.
**What I learned:** 
    - The different types of design aesthetics in web development. 
    - What actually differentiates a design from the typical AI slop to something unique, tailored specifically to certain design requirements, customer and administrative needs.
    - How to convert from one design type to another while still trying to maintain the original architecture, routing and types.
**Blockers / What I'm stuck on:** None
**Plan for tomorrow:** Work on the unit tests for audit engine (wrong plan, cheaper alternative, already-optimal case). Start with the first user interview. Integrate Anthropic API for personalized summary.

## Day 4 — 2026-05-09
**Hours worked:** 5
**What I did:** 
    - The "overpaying" rendered solid gray instead of black outline. Fixed by using React style property.
    - AuditResults.tsx was reading fields that don't exist on the AuditResult type. The fix corrects all field names and now iterates audit.findings, filtering to only show overspending and suboptimal entries (hiding optimal ones). Each finding card now shows the tool name, current plan, current spend, the recommendation, the detailed reasoning, and the savings, plus a "Full Stack Overview" table so every tool is accounted for.
    - Fixed the AI summary prompt which now includes a full per-tool breakdown with current plan, spend, status, and recommendation for each tool.
    - Added unit tests for the audit engine.
**What I learned:** 
    - Using DevTools to inspect root causes.
    - Data contract failures are silent when the frontend reads one key while the engine produces a different key as  output.
**Blockers / What I'm stuck on:** The PDF export is most probably broken by oklch colours generated by Tailwind v4 which html2canvas can't render.
**Plan for tomorrow:** Fix the PDF export option. Note the user interview 1 in the docs. Run the unit tests and note the finding in docs. Refine UI for Audit results page and Shared Audit page.

## Day 5 — 2026-05-10
**Hours worked:** 6
**What I did:** 
    1. Vercel deploy failure: Missing dependencies
        - Issue: `react-router-dom`, `lucide-react`, `@supabase/supabase-js`, etc. installed locally but not in the `package.json`
        - Cause: Ran `npm install <pkg>` without `--save` on day 1
        - Fix: `npm install react-router-dom lucide-react @supabase/supabase-js --save`
    2. Test file import errors
        - Issue: `import { runAudit } from '../src/lib/auditEngine';` failed
        - Cause: From `src/tests`, `../` already points to `src/`. Extra `src/` made path `src/src/lib` which doesn't exist. 
        - Fix: Changed to `import { runAudit } from '../lib/AuditEngine'`
        - Fix: Changed to `import type { AuditInput } from '../types/index.ts';`
    3. Vitest not recognized
        - Issue: `"test": "vitest run"` script existed, but package wasn't installed
        - Cause: Planned to add testing later and forgot to run `npm install`
        - Fix: `npm install -D vitest` to add to `devDependencies`
    4.  UI changed in results page
        - Added lighter mode palette colors to maintain overall consistency
    5. Added the `README.md` file.
**What I learned:** 
    - *Dependencies must be in `package.json`, not just `node_modules`*
        Local `npm install` without `--save` works on local machine but breaks when Vercel builds it. When importing, it must be in `dependencies` or `devDependencies`. `node_modules` is gitignored.
    - *Relative imports are relative to the current file*
        `../` = `src/`. To check, count folders of the file and add one `../` per folder level
    - *`package.json` scripts don't install packages*
        Adding `"test": "vitest run"` doesn't auto-install `vitest`. Always install the tool before adding the script.
    - *Tailwind color scales: 50-200 = light, 800-950 = dark*
        Light UIs use low numbers for backgrounds `bg-color-50`, mid numbers for borders `border-color-200`, high numbers for text `text-color-700`
**Blockers / What I'm stuck on:** The PDF export is most probably broken by oklch colours generated by Tailwind v4 which html2canvas can't render.
**Plan for tomorrow:** Fix the share audit logic and export PDF option. Add user 2 interview in the docs. Get started with the engineering files and research for the entrepreneurial files. 

## Day 6 — 2026-05-11
**Hours worked:** 9
**What I did:** 
    - Fixed CI test failure
        Anthropic API threshold bug where $500 returned `suboptimal` instead of `overspending`. Wrong logic for `ci.yml` was pushed when the `audit.test.ts` was running correctly locally.
    - Added user interview docs
        Documented 3 real conversations with potential users. Captured pain points around AI tool sprawl and manual cost tracking.
    - Wrote `ECONOMICS.md`covering unit economics
    - Improved accesibility for "next steps" card.
    - Fixed mobile layout issues on audit results page by removing `max-width` constarint and adjusted responsive padding.
    - Updated `.gitignore` for Vite artifacts and cleaned up track builds.
    - Added CI workflow `.github/workflows/ci.yml` to run audit engine tests on push/PR. Initial run failed due to test issue above, then fixed.
    - Added `METRICS.md` with North Star metric.
    - Added `LANDING_COPY.md` with hero headline, primary CTA, social proof, and FAQs.
**What I learned:** 
    - *CI workflow depends on even the minute edge cases*
        After changing the edge case for overspending rule in the `audit.test.ts`, `ci.yml` was running for the wrong test after it was pushed. But after reverting the changes all the tests sucessfully worked.
    - *We often don't realize stuffs that are infront of us until someone else points it out*
        While conversing with people who use AI regularly in their build or make process, they don't even know they're overspending until they saw the monthly total. The "aha" moment that followed after the aggregated number , not individual tool prices.
    - *Supabase Policies*
        RLS policies + anon insert is the right pattern for shareable public audits with no PII. 
**Blockers / What I'm stuck on:** PDF export still broken by oklch colours generated by Tailwind v4 which html2canvas can't render.
**Plan for tomorrow:** Fix PDF export and any other bugs or inconsistencies. Add Prompts documentation. Write project reflections.

## Day 7 — 2026-05-12
**Hours worked:** 9
**What I did:** 
    - Finished MVP end-to-end
        Spend input form → Tool usage, right plan, and cheaper alternatives evaluation → Audit results displayed eith AI analysis → localSt
    - Made website fully responsive
        Mobile layouts, hamburger menu, tested on both desktop/mobile
    - Overhauled `README.md`
        Added live demo with 4 screenshots
    - Fixed git push issues with large PNGs using `http.postBuffer`
    - Debugged broken README images
        Root cause was missing open quotes in all 4 `<img src>` tags
    - Debugged the mobile responsive UI
        Root cause was that the `index.css` did not have the minimum width for the body declared
    - Added the connection for lead capture between `AuditResults.tsx` with Supabase
**What I learned:** 
    - *HTML is unforgiving*
        One missing `"` broke all 4 images and keeps giving misleading 404s.
    - *1.3MB PNGs are overkill for README docs*
        Fails to push to remote origin.
    - *Adding min width is essential in `index.css`*
        Always make surec to add min width accordingly to ensure device responsiveness and text or container overflow when switching from Desktop view to Mobile view.
**Blockers / What I'm stuck on:** PDF export deffered